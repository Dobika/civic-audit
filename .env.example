# Anthropic Claude API key (for AI analysis features)
ANTHROPIC_API_KEY=

# Python interpreter path (defaults to "python3" if not set)
PYTHON_PATH=python3

# Directory paths (defaults shown; override if needed)
DATA_RAW_DIR=data/raw
DATA_PROCESSED_DIR=data/processed
